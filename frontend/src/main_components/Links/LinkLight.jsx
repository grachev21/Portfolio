const LinkLight = ({ link }) => {
  return <main className="text-colorLinkLight font-normal 
                          text-2xl cursor-pointer
                          tablet:text-base laptop:text-lg">{link}</main>;
};
export default LinkLight;
